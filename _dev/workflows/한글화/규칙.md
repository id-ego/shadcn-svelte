# 한글화 작업 규칙

## 기본 설정

- **작업 대상 폴더**: `docs/`
- **커밋 전 반드시 사용자 확인 필요**
- **ko 에이전트를 통해 작업 진행**
- **체크리스트 업데이트 필수**

## 제외 대상

### 폴더 제외

| 경로                      | 사유                                     |
| ------------------------- | ---------------------------------------- |
| `docs/.svelte-kit/`       | 자동 생성 파일                           |
| `docs/.velite/`           | 자동 생성 파일                           |
| `docs/.wrangler/`         | Cloudflare 설정                          |
| `docs/node_modules/`      | 의존성 패키지                            |
| `docs/static/`            | 이미지, 폰트, 스키마 등 정적 파일        |
| `docs/scripts/`           | 빌드 스크립트                            |
| `docs/src/__registry__/`  | 자동 생성 레지스트리                     |
| `docs/src/lib/registry/`  | UI 컴포넌트 라이브러리 (핵심 라이브러리) |
| `docs/src/lib/hooks/`     | 훅 라이브러리                            |
| `docs/src/params/`        | SvelteKit 파라미터                       |
| `docs/src/routes/api/`    | API 라우트 (UI 없음)                     |
| `docs/src/routes/og/`     | OG 이미지 생성                           |
| `docs/src/routes/repro/`  | 재현 라우트                              |
| `docs/src/routes/(view)/` | 블록 뷰 (iframe 콘텐츠)                  |

### 파일 제외

| 패턴                         | 사유             |
| ---------------------------- | ---------------- |
| `*.config.js`, `*.config.ts` | 설정 파일        |
| `*.json`                     | 데이터/설정 파일 |
| `+server.ts`                 | 서버 라우트      |
| `+layout.server.ts`          | 서버 레이아웃    |
| `schemas.ts`, `data.ts`      | 데이터 스키마    |
| `index.ts`                   | 모듈 내보내기    |

## 한글화 대상

### 번역 O

- `docs/content/` - 마크다운 문서 (본문, 제목, 설명)
- `docs/src/lib/components/` - 사이트 UI 컴포넌트
- `docs/src/lib/navigation.ts` - 네비게이션 메뉴
- `docs/src/routes/(app)/` - 앱 라우트 페이지 (UI 텍스트)
- `docs/src/app.html` - HTML 메타데이터

### 번역 X (원본 유지)

- 코드 예제 내부 텍스트
- API 명세 (props, events, slots)
- 파일명, 폴더명
- import 문, 변수명
- CLI 명령어
- URL, 경로
- **컴포넌트 이름 (title) - 전체 영어 유지**
- 브랜드명 (GitHub, Svelte 등)

### 컴포넌트 이름 규칙

**모든 컴포넌트 이름은 영어로 유지한다.**

| 유형 | 규칙 | 예시 |
| ---- | ---- | ---- |
| 단일 컴포넌트 | 영어 유지 | Button, Card, Input, Table |
| 복합 컴포넌트 | 영어 유지 | Alert Dialog, Data Table, Dropdown Menu |
| 설명적 이름 | 영어 유지 | Date Picker, Scroll Area, Hover Card |

**이유:**
- 개발자 친숙도 (업계 표준 용어)
- 코드와 일치 (`<Button>`, `<AlertDialog>`)
- 일관성 유지

## 용어집

| 영어            | 한글         |
| --------------- | ------------ |
| Component       | 컴포넌트     |
| Installation    | 설치         |
| Getting Started | 시작하기     |
| Documentation   | 문서         |
| Dark Mode       | 다크 모드    |
| Theming         | 테마 설정    |
| Usage           | 사용법       |
| Example         | 예제         |
| API Reference   | API 레퍼런스 |
| Note            | 참고         |
| Warning         | 주의         |
| Tip             | 팁           |
| Preview         | 미리보기     |
| Copy            | 복사         |
| Search          | 검색         |
| Submit          | 제출         |
| Cancel          | 취소         |
| Save            | 저장         |
| Delete          | 삭제         |
| Edit            | 편집         |
| Settings        | 설정         |
| Login           | 로그인       |
| Sign up         | 회원가입     |
| Logout          | 로그아웃     |

## 작업 순서

1. 체크리스트에서 미완료 항목 확인
2. ko 에이전트로 작업 지시
3. 작업 완료 후 체크리스트 업데이트
4. **사용자에게 커밋 여부 확인**
5. 승인 후 커밋 진행

## 에이전트 사용법

```bash
Task tool 호출:
- subagent_type: "ko"
- prompt: 파일 경로와 번역 대상 명시
- 체크리스트 업데이트 지시 포함
```

## 주의사항

- 문장은 자연스러운 한국어로 번역
- 기술 용어는 용어집 참조
- 코드 예제의 주석도 번역하지 않음
- 병렬 작업 시 파일 충돌 주의
- **navigation.ts 동기화 필수**: 콘텐츠 파일(index.md)의 title을 번역하면 `navigation.ts`의 find/filter 조건도 함께 수정해야 함
  - 예: `doc.title === "Migration"` → `doc.title === "마이그레이션"`
